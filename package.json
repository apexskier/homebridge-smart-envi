{
  "displayName": "Smart Envi",
  "name": "@apexskier/homebridge-smart-envi",
  "version": "1.0.6",
  "description": "Control the Eheat Smart Envi heater in homebridge.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/apexskier/homebridge-smart-envi.git"
  },
  "bugs": {
    "url": "https://github.com/apexskier/homebridge-smart-envi/issues"
  },
  "funding": [
    {
      "type": "github",
      "url": "https://github.com/sponsors/apexskier"
    }
  ],
  "engines": {
    "node": "^20.9.0, ^22.0.0, ^24.0.0",
    "homebridge": "^1.6.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "lint": "prettier --check . && eslint src/**.ts --max-warnings=0",
    "fix": "prettier --write . && eslint src/**.ts --fix",
    "watch": "npm run build && npm link && nodemon",
    "build": "rimraf ./dist && tsc",
    "prepare": "npm run lint && npm run build"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "devDependencies": {
    "@types/color-convert": "^2.0.3",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "eslint": "^8.45.0",
    "eslint-config-prettier": "^9.0.0",
    "homebridge": "^1.6.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "color-convert": "^2.0.1"
  }
}
